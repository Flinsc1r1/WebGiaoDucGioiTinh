@model IReadOnlyList<QuizIndexRow>
@{
    ViewData["Title"] = "Quiz";
}

<div class="mb-4 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
    <p class="text-sm text-slate-600">@Model.Count câu hỏi</p>
    <a asp-action="Create" class="inline-flex items-center justify-center rounded-lg bg-slate-900 px-4 py-2 text-sm font-medium text-white hover:bg-slate-800">+ Thêm câu hỏi</a>
</div>

<div class="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm">
    <div class="overflow-x-auto">
        <table class="min-w-full divide-y divide-slate-200 text-sm">
            <thead class="bg-slate-50">
                <tr>
                    <th scope="col" class="px-4 py-3 text-left text-xs font-semibold uppercase tracking-wide text-slate-600">Id</th>
                    <th scope="col" class="px-4 py-3 text-left text-xs font-semibold uppercase tracking-wide text-slate-600">Câu hỏi</th>
                    <th scope="col" class="px-4 py-3 text-left text-xs font-semibold uppercase tracking-wide text-slate-600">Bài học</th>
                    <th scope="col" class="px-4 py-3 text-center text-xs font-semibold uppercase tracking-wide text-slate-600">Đúng</th>
                    <th scope="col" class="px-4 py-3 text-right text-xs font-semibold uppercase tracking-wide text-slate-600">Thao tác</th>
                </tr>
            </thead>
            <tbody class="divide-y divide-slate-100 bg-white">
                @foreach (var row in Model)
                {
                    <tr class="hover:bg-slate-50/80">
                        <td class="whitespace-nowrap px-4 py-3 font-mono text-slate-600">@row.Id</td>
                        <td class="max-w-md px-4 py-3 text-slate-900">@row.Question</td>
                        <td class="whitespace-nowrap px-4 py-3 text-slate-600">@row.LessonTitle</td>
                        <td class="whitespace-nowrap px-4 py-3 text-center font-semibold text-teal-800">@row.CorrectAnswer</td>
                        <td class="whitespace-nowrap px-4 py-3 text-right">
                            <a asp-action="Edit" asp-route-id="@row.Id" class="font-medium text-teal-700 hover:underline">Sửa</a>
                            <span class="mx-2 text-slate-300">|</span>
                            <a asp-action="Delete" asp-route-id="@row.Id" class="font-medium text-red-600 hover:underline">Xóa</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
